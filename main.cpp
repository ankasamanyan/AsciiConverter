#include "AsciiConverter.hpp"
#include <iostream>
#include <fstream>

int main()
{
	AsciiConverter atrsie("ghost.xpm"); 
	// int num = 16776957;
	// colorConverter(num);
  	// struct RGB rgbColor;
	// rgbColor.r = ((num >> 16) & 0xFF);  // Extract the RR byte
	// //   std::cout << RED << "RED: " << rgbColor.r << RESET_LINE;
	// rgbColor.g = ((num >> 8) & 0xFF);   // Extract the GG byte
	// //   std::cout << GREEN << "GREEN: " << rgbColor.g << RESET_LINE;
 	// rgbColor.b = ((num) & 0xFF);        // Extract the BB byte
	// //   std::cout << BLUE << "BLUE: " << rgbColor.b << RESET_LINE;
	// std::string stringy =  "\033[42;2;";
	// stringy.append(std::to_string(rgbColor.r)).append(";").append(std::to_string(rgbColor.g)).append(";").append(std::to_string(rgbColor.b)).append("m").append(" ");
	// std::cout << stringy << RESET_LINE;
	// std::string str = "0x9C82BA";
    // if (inputFile.is_open()) 
	// {
	// 	std::string line;
    //     while (std::getline(inputFile, line)) 
    //         std::cout << line << std::endl;
    //     inputFile.close();
    // } else {
    //     // Failed to open the file
    //     // Handle the error here
    // }
	// printMsg("center me", LILY);

	return 0;
}
